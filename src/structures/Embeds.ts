import { MessageEmbed } from "discord.js";

export const feedbackSubmittedEmbed = new MessageEmbed({
    "description":"Your feedback has successfully been submitted! Responses will appear below."
})

export const submittingFeedbackEmbed = new MessageEmbed({
    "description":"Submitting your feedback. Please wait a moment..."
})